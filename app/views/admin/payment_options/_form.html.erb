<%= form_for([:admin, @payment_option]) do |f| %>
  <% if @payment_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_option.errors.count, "error") %> prohibited this admin_payment_option from being saved:</h2>

      <ul>
      <% @payment_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :duration,"Duration In Months" %> <br />
    <%= f.text_field :duration %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
